defaults:
  - default_arch_params

structure: # trainer structure
num_classes: # number of outputs of the classification head
width_mult: # trainer's width multiplier
dropout: # dropout probability of classifier fully-connected layer